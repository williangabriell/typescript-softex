const base = Number(prompt("Digite a base do retângulo: "))
const alturaRetangulo = Number(prompt("Digite a altura do retângulo: "))

const area = base * alturaRetangulo
console.log(`A área desse retângulo é de: ${area}`)