let diaSemana = (prompt('Digite o dia da semana por extenso (Ex: segunda)'))

switch (diaSemana) {
    case 'segunda':
        console.log(`${diaSemana} é um dia útil`)
        break;
    case 'terça':
        console.log(`${diaSemana} é um dia útil`)
    case 'quarta':
        console.log(`${diaSemana} é um dia útil`)
        break;
    case 'quinta':
        console.log(`${diaSemana} é um dia útil`)
        break;
    case 'sexta':
        console.log(`${diaSemana} é um dia útil`)
        break;
    case 'sábado':
        console.log(`${diaSemana} é um fim de semana`)
        break;
    case 'domingo':
        console.log(`${diaSemana} é um fim de semana`)
        break;
}